# Backend API base URL
# Important: The backend is configured with a global prefix "/api".
# So make sure the URL you set here already ends with "/api".
# Example for local development:
# VITE_API_URL=http://localhost:8080/api

VITE_API_URL=